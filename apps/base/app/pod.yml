---
apiVersion: v1
kind: Pod
metadata:
  labels:
    run: demo
  name: demo
spec:
  containers:
  - image: alpine
    name: c1
    command:
      - tail
      - -f
      - /dev/null
  restartPolicy: Always

